# This is script to ask user to run 
    # User can run server , client or both 
    # user can build docker image and run it 
        # user can run docker-compose up -d
        # user can run docker-compose down
        # docker push image to docker-hub


# This script should modify the values.yaml file in devops/level-3/helm based on level provided(dev, devops or testing)
# This script to be modify devops, development or testing  based on type (dev, devops or testing)
# This script should also push , create pr and merge into dev branch after changes.

# This script should also create a docker image and push it to docker-hub

replicaCount: 1
image:
  repository: harishdell/els-client
  tag: "0.0.5-10"
  pullPolicy: IfNotPresent
service:
  type: ClusterIP
  port: 80
  targetPort: 80
ingress:
  enabled: true
  className: "nginx"
  annotations: {}
  hosts:
    - host: els-client.local
      paths:
        - path: /
          pathType: Prefix
# Server URL that will be passed to React app at build time
serverUrl: "http://dev.els-api.local"
env: {}

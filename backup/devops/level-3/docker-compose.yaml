services:
  strapi:
    build:
      context: ../../development/level-3/server
      dockerfile: Dockerfile
    container_name: strapi-cms
    ports:
      - "1337:1337"
    environment:
      NODE_ENV: development
    volumes:
      - ../../development/level-3/server:/opt/app
      - node_modules:/opt/app/node_modules
      - strapi_db:/opt/app/.tmp
    restart: unless-stopped

volumes:
  node_modules:
  strapi_db:
